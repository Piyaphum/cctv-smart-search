# 🕵️‍♂️ CCTV Smart Search (Person Re-Identification)

ระบบค้นหาบุคคลเป้าหมายในกล้องวงจรปิดด้วย AI อัจฉริยะ พร้อมระบบแจ้งเตือนทางอีเมลอัตโนมัติ

---

## 🛠️ Tech Stack (เทคโนโลยีที่ใช้)

โปรเจคนี้พัฒนาโดยใช้ภาษา **Python** 100% โดยมีการเลือกใช้ Library และ Framework ชั้นนำทางด้าน AI ดังนี้:

* **Frontend / UI:**
    * `Streamlit`: ใช้สร้าง Web Application Interface ที่ใช้งานง่ายและตอบสนองไว
* **Computer Vision & AI Core:**
    * `YOLOv8` (Ultralytics): โมเดลตรวจจับวัตถุที่รวดเร็วและแม่นยำ ใช้สำหรับตรวจจับ "มนุษย์" ในวิดีโอ
    * `ResNet50` (PyTorch): โมเดล Deep Learning สำหรับสกัดคุณลักษณะเด่น (Feature Extraction) เพื่อใช้ระบุตัวตนบุคคล (Re-ID)
    * `OpenCV`: ใช้สำหรับจัดการไฟล์วิดีโอ ตัดภาพ และประมวลผลภาพเบื้องต้น
* **Data Processing:**
    * `NumPy` & `Pandas`: ใช้คำนวณทางคณิตศาสตร์และจัดการข้อมูล Log การตรวจจับ
* **System & Utilities:**
    * `SMTP`: ระบบส่งอีเมลแจ้งเตือนอัตโนมัติ

---

## ⚙️ หลักการทำงาน (System Workflow)

ระบบมีการทำงานแบบ **Hybrid Matching** ซึ่งผสมผสานระหว่าง "รูปร่าง" และ "สีเสื้อ" เพื่อความแม่นยำสูงสุด:

1.  **Target Analysis (วิเคราะห์เป้าหมาย):**
    * เมื่อผู้ใช้อัปโหลดรูปคนร้าย ระบบจะใช้ **ResNet50** แปลงรูปภาพเป็น "เวกเตอร์ทางคณิตศาสตร์" (Embeddings)
    * ระบบจะทำการวิเคราะห์ "Histogram สีเสื้อ" แยกต่างหาก เพื่อจดจำสีเสื้อผ้าที่สวมใส่

2.  **Video Scanning (สแกนวิดีโอ):**
    * ระบบอ่านไฟล์วิดีโอและใช้ **YOLOv8** ตรวจจับตำแหน่งของ "คน" ในแต่ละเฟรม
    * ทำการตัดภาพ (Crop) เฉพาะตัวคนออกมาเพื่อประมวลผลต่อ

3.  **Feature Matching (การจับคู่):**
    * นำภาพคนที่เจอในวิดีโอมาเปรียบเทียบกับภาพเป้าหมาย
    * **เกณฑ์ที่ 1:** เปรียบเทียบโครงสร้างร่างกายด้วย *Cosine Similarity*
    * **เกณฑ์ที่ 2:** เปรียบเทียบสีเสื้อด้วย *Histogram Correlation*
    * นำคะแนนทั้งสองส่วนมาคำนวณร่วมกัน (Weighted Score)

4.  **Result & Alert (ผลลัพธ์และการแจ้งเตือน):**
    * หากคะแนนความเหมือนสูงกว่าเกณฑ์ (Threshold) ระบบจะนับว่าเป็น "เป้าหมาย"
    * แสดงผลรูปภาพและเวลาที่พบบนหน้าจอ
    * เมื่อประมวลผลเสร็จสิ้น ระบบจะรวบรวมรูปภาพทั้งหมดส่งเข้า **Email** ของผู้ใช้ทันที

---

## 💻 คู่มือการติดตั้ง (Installation Guide)

กรุณาทำตามขั้นตอนทีละข้อเพื่อติดตั้งระบบลงในเครื่องคอมพิวเตอร์

### 1. สิ่งที่ต้องมีก่อน (Prerequisites)
* **Python:** (เวอร์ชัน 3.10 หรือ 3.11 แนะนำที่สุด) [ดาวน์โหลดที่นี่](https://www.python.org/downloads/)
* **Git:** (สำหรับดาวน์โหลดโค้ด) [ดาวน์โหลดที่นี่](https://git-scm.com/downloads)
* **VS Code:** (หรือ Text Editor อื่นๆ)

### 2. ขั้นตอนการติดตั้ง (Step-by-Step)

   **ขั้นตอนที่ 1: ดาวน์โหลดโค้ดจาก GitHub**
      เปิด Terminal (หรือ CMD) แล้วพิมพ์คำสั่ง:
      
      ```bash
            git clone [https://github.com/Piyaphum/cctv-smart-search.git](https://github.com/Piyaphum/cctv-smart-search.git)
            cd cctv-smart-search

   **ขั้นตอนที่ 2: สร้างจำลองสภาพแวดล้อม (Virtual Environment) ขั้นตอนนี้สำคัญ! เพื่อป้องกันไม่ให้ Library ตีกับโปรเจคอื่นในเครื่อง**
      
      ```bash
         python -m venv venv
         venv\Scripts\activate

   **ขั้นตอนที่ 3: ติดตั้ง Library ที่จำเป็น คำสั่งนี้จะดาวน์โหลด AI Model และเครื่องมือทั้งหมดตามไฟล์**
      ```bash
      pip install -r requirements.txt

## ⚙️ การตั้งค่าอีเมล (Email Configuration)
   สำคัญ: ก่อนเริ่มใช้งาน คุณต้องแก้ไขโค้ดเพื่อตั้งค่าอีเมลผู้ส่ง (Sender) ให้เป็นของคุณเอง ไม่อย่างนั้นระบบแจ้งเตือนจะไม่ทำงาน
   1. เปิดไฟล์ app.py ด้วย VS Code หรือ Notepad
   2. ค้นหาบรรทัดที่เขียนว่า sender_email (ประมาณบรรทัดที่ 124)
   3. แก้ไขข้อมูลดังนี้

   # แก้ไขข้อมูลในส่วนนี้ให้เป็นของคุณ
      sender_email = "your_email@gmail.com"        # ใส่อีเมล Gmail ของคุณที่จะใช้ส่ง
      sender_password = "xxxx xxxx xxxx xxxx"      # ใส่ App Password 16 หลัก (ไม่ใช่รหัสผ่านล็อกอินปกติ)

## วิธีขอ App Password ของ Gmail:
   1. ไปที่ Google Account > Security (ความปลอดภัย)
   2. เปิดใช้งาน 2-Step Verification (การยืนยันแบบ 2 ขั้นตอน)
   3. ค้นหาคำว่า "App passwords" (รหัสผ่านแอป)
   4. สร้างรหัสใหม่ ตั้งชื่อว่า "CCTV App" แล้วนำรหัส 16 ตัวที่ได้มาใส่ในโค้ด

## วิธีการรันโปรแกรม (How to Run)
   1. รันคำสั่งใน Terminal

      ```bash
      streamlit run app.py
   
   3. เริ่มใช้งาน
         โปรแกรมจะเปิดหน้าเว็บขึ้นมาให้เองโดยอัตโนมัติ (ปกติจะอยู่ที่ http://localhost:8501)
         หากเว็บไม่เด้งขึ้นมา ให้ก๊อปปี้ URL http://localhost:8501 ไปวางใน Google Chrome

## 📖 คู่มือการใช้งาน (User Manual)
   1. ตั้งค่าเป้าหมาย (Sidebar ด้านซ้าย):
         กดปุ่ม Browse files แล้วเลือกรูปภาพของ "คนร้าย" หรือเป้าหมายที่ต้องการหา
         (Optional) ติ๊กถูกที่ Enable Email Report? และใส่ Email ปลายทาง หากต้องการรับรายงานผล
   2. อัปโหลดวิดีโอ (หน้าจอหลัก):
         กดปุ่ม Browse files เพื่อเลือกไฟล์วิดีโอ CCTV (เลือกได้หลายไฟล์พร้อมกัน)
         สามารถปรับค่า AI Threshold ได้ (ค่าแนะนำคือ 0.70 หากต้องการความแม่นยำสูงให้เพิ่มค่าขึ้น)
   3. เริ่มค้นหา:
         กดปุ่มสีแดง 🚀 Start Search
         ระบบจะแสดงผลลัพธ์คนที่หน้าตาคล้ายเป้าหมายขึ้นมาทันทีที่เจอ
         เมื่อครบทุกวิดีโอ ระบบจะสรุปยอดและส่งอีเมลแจ้งเตือนพร้อมรูปภาพ

## ⚠️ หมายเหตุ (Important Note)
   1. Performance: ความเร็วในการประมวลผลขึ้นอยู่กับความละเอียดของวิดีโอและสเปกคอมพิวเตอร์ (แนะนำให้ใช้เครื่องที่มี GPU NVIDIA จะเร็วขึ้นมาก)
   2. Storage: ระบบมีการจัดการหน่วยความจำแบบ Chunking ทำให้สามารถรองรับไฟล์วิดีโอขนาดใหญ่ได้โดยที่เครื่องไม่ค้าง แต่ต้องมีพื้นที่ว่างใน Drive D (หรือ Drive หลัก) เพียงพอสำหรับการพักไฟล์ชั่วคราว
