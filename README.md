# 🕵️‍♂️ CCTV Smart Search (Simple Mode)

ระบบค้นหาบุคคลในภาพจากกล้องวงจรปิดด้วย AI (YOLOv8 + ResNet50) พร้อมระบบส่งรายงานทางอีเมล

## ✨ ฟีเจอร์หลัก
* **Target Search:** อัปโหลดรูปคนร้าย (Target) เพื่อค้นหาในวิดีโอ
* **Multi-Video Upload:** รองรับการอัปโหลดไฟล์วิดีโอทีละหลายไฟล์
* **Safe Upload Mode:** มีระบบจัดการหน่วยความจำ (Chunking) พักไฟล์ลง Drive D: ป้องกัน RAM เต็ม หรือ Drive C เต็ม
* **Email Reporting:** ส่งสรุปผลพร้อมรูปภาพ (Snapshot) เข้าอีเมลเมื่อค้นหาเสร็จสิ้น
* **Smart Detection:** ใช้เกณฑ์ผสมระหว่างรูปร่าง (Re-ID) และสีเสื้อ (Color Histogram)

---

## 🛠️ สิ่งที่ต้องติดตั้งก่อน (Prerequisites)

1.  **Python 3.8 ขึ้นไป** (แนะนำ 3.10 หรือ 3.11)
2.  **Git** (สำหรับดาวน์โหลดโค้ด)
3.  **พื้นที่ว่างใน Drive D:** โปรแกรมถูกตั้งค่าให้ใช้ `D:\person-reid\temp_video` เป็นที่พักไฟล์ชั่วคราว

---

## 🚀 วิธีติดตั้ง (Installation)

1.  **ดาวน์โหลดโปรเจค**
    ```bash
    git clone [https://github.com/Piyaphum/cctv-smart-search.git](https://github.com/Piyaphum/cctv-smart-search.git)
    cd cctv-smart-search
    ```

2.  **ติดตั้ง Library ที่จำเป็น**
    เปิด Terminal หรือ CMD ในโฟลเดอร์โปรเจค แล้วพิมพ์:
    ```bash
    pip install -r requirements.txt
    ```
    *(ขั้นตอนนี้อาจใช้เวลาสักพัก เพราะต้องโหลด PyTorch และ YOLO)*

---

## ▶️ วิธีใช้งาน (Usage)

1.  **รันโปรแกรม**
    พิมพ์คำสั่ง:
    ```bash
    streamlit run app.py
    ```

2.  **ตั้งค่าเป้าหมาย (Sidebar ด้านซ้าย)**
    * อัปโหลดรูป "คนร้าย" หรือเป้าหมายที่ต้องการค้นหา
    * (Optional) ติ๊กถูกที่ `Enable Email Report?` หากต้องการให้ส่งเมล
    * ใส่อีเมลปลายทางในช่อง `Recipient Email`

3.  **อัปโหลดวิดีโอ (หน้าจอหลัก)**
    * กดปุ่ม **Browse files** เพื่อเลือกไฟล์วิดีโอ CCTV (เลือกได้หลายไฟล์พร้อมกัน)
    * ปรับค่าความเข้มงวด (Threshold) ได้ตามต้องการ

4.  **เริ่มค้นหา**
    * กดปุ่ม **🚀 Start Search**
    * ระบบจะค่อยๆ ประมวลผลและแสดงรูปคนที่เจอแบบ Real-time
    * เมื่อเสร็จสิ้น หากเจอเป้าหมาย ระบบจะส่งอีเมลรายงานทันที

---

## ⚙️ การตั้งค่าอีเมล (Email Config)

ในไฟล์ `app.py` มีการตั้งค่าอีเมลผู้ส่งไว้อยู่แล้ว หากต้องการเปลี่ยน ให้แก้ไขที่บรรทัดประมาณ 120-125:

```python
# แก้ไขในไฟล์ app.py
sender_email = "your_email@gmail.com" 
sender_password = "your_app_password"  # รหัส App Password 16 หลัก
